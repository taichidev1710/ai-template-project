/** Central route path constants. Never hardcode a path string in components. */
export const paths = {
  root: '/',
  login: '/login',
  dashboard: '/dashboard',
  users: '/users',
  profile: '/profile',
  blockTypes: '/block-types',
} as const;
