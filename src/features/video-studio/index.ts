/**
 * Video Studio feature — public API (spec §13). Import ONLY from here.
 * P1: a UI skeleton driven by a fake provider; server state (Project ↔ MongoDB)
 * arrives in P2.
 */
export { VideoStudioPage } from './pages/VideoStudioPage';
